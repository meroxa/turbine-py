[tox]
isolated_build = True
envlist = python3{11, 10, 9}
skip_missing_interpreters = true

[testenv]
deps =
    -r{toxinidir}/requirements-dev.txt
    -r{toxinidir}/src/turbine/function_deploy/function_app/requirements.txt

commands = pytest -v --tb=short
